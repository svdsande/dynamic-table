import { CellTemplateMarkerDirective } from './cell-template-marker.directive';

describe('CellTemplateMarkerDirective', () => {
  it('should create an instance', () => {
    const directive = new CellTemplateMarkerDirective();
    expect(directive).toBeTruthy();
  });
});
